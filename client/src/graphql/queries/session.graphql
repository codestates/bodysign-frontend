query Session($id: Int!) {
	session(id: $id) {
		id
		userId
		trainerId
		feedback
		sentFeedback
		date
		sessionExercises {
			id
			name
			sessionExerciseVolumes {
				id
				reps
				sets
				weight
				seq
			}
		}
	}
}
